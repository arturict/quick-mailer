# Production Environment Configuration
# Copy this file to .env and configure for your production environment

# ==========================================
# ENVIRONMENT
# ==========================================
NODE_ENV=production

# ==========================================
# EMAIL PROVIDER CONFIGURATION
# ==========================================

# Email Provider: 'resend' or 'smtp'
EMAIL_PROVIDER=resend

# --- Resend API Configuration (if EMAIL_PROVIDER=resend) ---
RESEND_API_KEY=re_your_production_api_key_here

# --- SMTP Configuration (if EMAIL_PROVIDER=smtp) ---
# SMTP_HOST=smtp.yourdomain.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=noreply@yourdomain.com
# SMTP_PASSWORD=your_secure_password_here

# ==========================================
# APPLICATION CONFIGURATION
# ==========================================

# From Addresses (comma-separated) - Required
FROM_ADDRESSES=noreply@yourdomain.com,support@yourdomain.com,info@yourdomain.com

# Default Sender Name
DEFAULT_SENDER_NAME=Your Company Name

# ==========================================
# SERVER CONFIGURATION
# ==========================================

# Server Port (internal)
PORT=3000

# Server Host (use 0.0.0.0 for Docker)
HOST=0.0.0.0

# Database Path
DATABASE_PATH=/app/backend/data/emails.db

# ==========================================
# DOCKER CONFIGURATION
# ==========================================

# External port to expose the application
EXTERNAL_PORT=3000

# Nginx ports (when using nginx profile)
HTTPS_PORT=443
HTTP_PORT=80
